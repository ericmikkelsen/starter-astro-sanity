// Sanity is the content source for all pages in this project.
// Pages are fetched via GROQ queries (src/sanity/queries.ts) and typed using
// generated types (src/sanity/types.ts). Astro content collections are not
// used for Sanity-sourced content.
export const collections = {};
